# Hypothesis ledger

| ID | Hypothesis | Evidence for | Disproof test | Status |
|---|---|---|---|---|
| H-001 | The current hooks conflate independent timing domains, so some states are gated twice while other per-update counters remain unscaled. | Confirmed independently in elapsed-time Notice/News waits, non-song input repeat, IFBL loop cardinality, and direct gameplay effect-frame writers. | Confirmed by the subsystem traces; correction design must be per sink/domain rather than one global scale. | Confirmed |
| H-002 | Menu input loss is caused by a mismatch between one-update game edge lifetime and the accepting callback's service cadence, rather than by the 1000 Hz worker's nominal polling frequency. | Edges are overwritten every native input update. | The old `sub_5A5E80` mapping was false; reassess only from the proven `SELECT_NOCARD` loop. | Superseded pending correct-path trace |
| H-003 | Gameplay judgement is already based primarily on an absolute audio/tune clock and should not be gated like visual effects. | The native Tune counter is converted to milliseconds before `0x005D68E0`, whose note/config comparisons and offsets are millisecond fields. | Confirmed; retain the target-rate frame-to-ms conversion and do not scale judgement windows. | Confirmed |
| H-004 | Prolonged transitions result from gating an update function whose internal progress is already elapsed-time based. | `CNoticeTask` is gated to 60 calls/sec, but its 2.0-second IFBL float wait subtracts the per-native-frame delta; the predicted duration is 4 seconds at 120 and 8 seconds at 240. | Confirm with runtime timestamps at task entry/notice labels, but the static clock mismatch is fully established. | Confirmed |
| H-005 | Accelerated non-song menu scrolling is driven by an unscaled higher-level per-update held-input repeat outside the currently patched repeat sites. | `0x00659110` uses unpatched thresholds 16 and 3 against a held duration advanced once per native update; customization/non-song menus call it directly, while song selection has a separate elapsed-time scheduler. | Confirmed; implementation design must preserve the 60 FPS initial/repeat wall times without reducing input sampling cadence. | Confirmed |
| H-006 | Whole-task 60 Hz gating of News/Notice creates mixed-clock failures in their IFBL scripts. | Confirmed for Notice's 2.0-second elapsed-time wait and News's 0.5/15.0/0.5-second elapsed-time waits. Integer waits would also be double-scaled if target-scaled and then consumed only at 60 task calls/sec. | Confirmed; replacement design must keep both tasks native-rate and adapt only the explicitly frame-counted operations inside them. | Confirmed |
| H-007 | Card-result presses are lost when their one-native-update edge occurs outside an accepting callback invocation. | The game edge lasts one update and the loader exports held level only. | `sub_5A5E80` is not the mandatory login page; trace the `sub_5A4540` descriptor loop before retaining or rejecting the edge-loss mechanism. | Superseded pending correct-path trace |
| H-008 | GREAT/GOOD and related judgement visuals are still accelerated because their renderer directly converts elapsed milliseconds with the newly shortened target-frame duration. | `0x006463F0` uses `Tune+0x18` for both asset lifetime and asset index, bypassing the gated effect manager. | Confirmed by tracing result publication through both renderer callsites and the exact FPU instructions. | Confirmed |
| H-009 | The current QPC-derived shared authored tick contributes visible cadence jitter at stable high FPS. | At 240 FPS small QPC interval variation can produce 3/5-native-frame spacing, while the target ratio is exactly one authored tick per four native updates. | Record authored-tick spacing from a fresh 240 FPS run and compare with an integer rational-phase clock. | Active; design improvement, not a proven primary root cause |
| H-010 | The 4x login countdown slowdown is caused by `CStartTask::Update` being serviced about 60 times/sec while consuming a roughly 1/240-second global delta. | The ratio would predict a 4x slowdown at 240 FPS. | E-031 proves CStartTask is on the normal native update traversal with no task-specific 60 Hz gate. | Disproved |
| H-011 | The login defect is a global opcode-1/CSeqTask service-boundary defect. | Multiple elapsed-time callbacks share the interpreter. | The login page uses opcode `0x27`, and sustained opcode-1 callbacks elsewhere were native-rate. | Disproved |
| H-012 | The mandatory login page is slowed because global IFBL integer-wait scaling treats its cooperative poll-loop yield as an authored duration. | The exact `SELECT_NOCARD` loop contains wait 1 between timer/input polling iterations; the current hook changes it to 4 at 240 FPS. | The instruction and descriptor path directly establish the four-update service interval. | Confirmed |
