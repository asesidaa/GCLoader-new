# Verified findings

Only evidence-backed facts belong here. Preliminary interpretations remain in
`HYPOTHESES.md`.

| ID | Domain | Finding | Evidence | Confidence |
|---|---|---|---|---|
| F-001 | Crash | The former loading crash was eliminated by relocating `EffectCadence16B`; the high-FPS behavior defects remain. | User runtime report, 2026-07-19 | High |
| F-002 | Runtime capture | The currently inspected `loader-log.txt` records a 60 FPS run, so it is not evidence for the reported 240 FPS behavior. | Startup line reports `target_fps=60` | High |
| F-003 | Patch architecture | The transformed patch uses several different techniques: constant substitution, duration scaling, frame-index mapping, and shared authored-60-Hz gating. | Current `FrameratePatch*` and `FramerateAuthoredClock*` source | High |
| F-004 | Analysis backend | The existing IDA daemon is open on `H:\gc\game471.exe.i64` with the `idalib` backend. | Required backend probe, 2026-07-19 | High |
| F-005 | Master clock | The main update phase and render phase are separate. The update path refreshes the global `CAppTimer` delta from `timeGetTime`; with the update phase running every presented frame, elapsed-time consumers receive the real per-frame delta rather than a fixed 60 Hz step. | Live IDA trace: `0x00458C10`, `0x00458B70`, `0x00458BB0`, `0x006317F0`, `0x006350E0`, `0x006350C0` | High |
| F-006 | Input edges/repeat | `0x00455C80` derives pressed/released bits by comparing current and previous held state, overwrites those edge fields on every input update, and decrements repeat counters once per input update. Pressed/released edges therefore have a one-update lifetime unless a consumer latches them. | Live IDA decompile and callers in E-006 | High |
| F-007 | Card flow input | The card/read/result flow is owned by `CStartTask`; its update at `0x005A40A0` calls `CSeqTaskBase_UpdateStateMachine` on every native task update. Its registered card/name confirmation callback `0x005A5E80` reads pressed edges, while name-entry cursor navigation `0x005A5F90` reads repeat bits. | RTTI plus live IDA traces E-008 and E-009 | High |
| F-008 | Loader input boundary | The loader's 1000 Hz worker publishes only the current held-level bitfield to the game-facing iDmac read. It does not retain a press/release transition queue for the game. | Current `InputSnapshotState` and iDmac read implementation | High |
| F-009 | MovieClip primitive | `0x004DF940` advances exactly one MovieClip timeline frame per call; its forward/backward wrappers dispatch through vtable offset `+0x150`. This proves the primitive is frame-counted, but not yet the cadence of every caller that reaches it indirectly. | Live IDA UI artifact E-005 | High |
| F-010 | Non-song menu repeat | `0x00659110` is a higher-level menu auto-repeat helper independent of the currently patched XIO/native-keyboard repeat constants. After an initial edge it compares the held-duration counter against `dword_782CE8 = 16`, then fires whenever `(duration - 16) % dword_782CEC == 0` with `dword_782CEC = 3`. The held duration advances in native update ticks, so at 240 FPS repeat begins after about 66.7 ms and recurs every 12.5 ms instead of the 60 FPS timings of about 266.7 ms and 50 ms. | Live IDA traces E-011 through E-014 | High |
| F-011 | Menu-family split | Customization/non-song menu handlers such as `0x005C1700` and `0x005C6650` call the frame-counted helper `0x00659110` directly. Song/list navigation uses a different path: `0x005AE770` queries pressed/held state and `0x005AEF00` schedules repeated movement with the global elapsed-time delta. This accounts for the reported difference between song selection and other menus. | Live IDA traces E-012 and E-014 | High |
| F-012 | IFBL wait domains | `0x006304B0` maintains both a float elapsed-time wait and an integer authored-frame wait for each lane. Float waits subtract the current global timer delta; integer waits decrement by one on every task update. Integer loop counts and callback retry delays are also native-update-counted. | Live IDA trace E-010 | High |
| F-013 | Legal-to-TAITO transition | The dumped `signature` XFL contains `jf_notice_start`/`tg_notice_start`, `jf_notice_end`/`tg_notice_end`, and TAITO labels. The live `CNoticeTask` descriptor uses those same notice labels and inserts an IFBL type `0x10` float wait of 2.0 seconds between notice-start and notice-end. | XFL E-015 and live descriptor E-016 | High |
| F-014 | Prolonged transition root cause | The current loader gates the complete `CNoticeTask::Update` at `0x006544D0` to an authored 60 Hz boolean. Inside it, IFBL reads the global delta produced every native update and subtracts that small delta only when the gated task runs. At target FPS `F`, a 2.0-second float wait therefore takes approximately `2.0 * F / 60` real seconds: 4 seconds at 120 FPS and 8 seconds at 240 FPS. The IDA comment claiming the whole-task gate is safe is contradicted by the live call/data flow. | Current hook source plus E-010 and E-016 | High |
| F-015 | News task mixed clock | The same whole-task error exists in `CNewsTask::Update` at `0x00618A50`. Its IFBL descriptor contains type `0x10` elapsed-time waits of 0.5, 15.0, and 0.5 seconds. Because the current loader calls the task only on authored-60-Hz ticks while each call subtracts the native-frame delta, those waits stretch by `target_fps / 60` (at 240 FPS: approximately 2, 60, and 2 seconds). | Current `NewsUpdate` hook source plus live descriptor E-018 | High |
| F-016 | Gameplay judgement | `Tune+0x10` is advanced by `Tune+0x14` at `0x00664E23` on every native gameplay update, with the step initialized to one. `0x006401E0` converts that frame to current milliseconds with `Tune+0x18`, invokes the judgement core at `0x005D68E0`, and that core compares note/config fields expressed in milliseconds. Changing `Tune+0x18` from `1000/60` to `1000/target_fps` while retaining native updates is therefore the correct high-resolution judgement clock; judgement windows and offsets must remain unscaled milliseconds. | Live IDA traces E-019 | High |
| F-017 | GREAT/GOOD effect root cause | Judgement publication stores the result at player-state `+0xF0` and its millisecond timestamp at `+0xF4`. The render helper `0x006463F0`, called at `0x0064F351` and `0x0064F479`, chooses the GREAT/GOOD-family asset and uses `Tune+0x18` both for `asset_frame_count * frame_ms` at `0x006464A8` and `(current_ms - judgement_ms) / frame_ms` at `0x00646528`. At 240 FPS the patched 4.1667 ms value makes an authored-60-Hz effect expire and advance four times as fast. | Live IDA traces E-020 and exact instructions E-021 | High |
| F-018 | Effect-manager coverage gap | `sub_5F08A0`/`sub_5F1740` advances managed effect state `+0x08` by one frame, and the current `GameplayEffectAdvance` hook correctly reduces that manager call to authored boundaries. Several gameplay render paths bypass the manager by directly overwriting effect `+0x08` from elapsed milliseconds divided by the target `Tune+0x18`; consequently the manager gate cannot correct GREAT/GOOD or those direct writers. | Live IDA traces E-020 through E-022 | High |
| F-019 | Additional authored-effect leaks | The same target-frame/asset-frame conflation occurs in paired lifetime/frame samplers at `0x00648F00`/`0x00648F8C` and `0x0064912B`/`0x006491E0`, a direct sampler at `0x00649C14`, chart-render effect samplers at `0x0064BC8B`, `0x0064CC8A`, `0x0064CCBE`, and `0x0064D836`, and a 50-frame visual timer at `0x00650AD5`. These sites consume authored asset-frame counts or fixed authored-frame lifetimes, whereas the surrounding chart/current-time multiplications remain target-frame conversions and must not be reverted. | Live IDA traces E-022 | High |
| F-020 | Countdown visual mismatch | The gameplay countdown is correctly expanded from 120 to `2 * target_fps` native ticks, but the renderer subtracts the remaining target ticks and writes that raw result directly to effect `+0x08` at `0x00649A9C`. At 240 FPS the effect receives four authored frames per original frame unless this final asset index is mapped back to 60 Hz. | Current direct patches plus live IDA trace E-022 | High |
| F-021 | IFBL loop semantics | IFBL decimal case 17 (`0x11`) stores the descriptor value into the integer wait at `0x006309D4`. Separately, decimal case 23 (`0x17`) pushes descriptor `+0x08` onto a loop stack at `0x00630AB6`, and case 24 (`0x18`) decrements that value and branches through the loop body. The current `IfblLoop` hook globally scales this control-flow repetition count as though it were a duration, changing script cardinality and double-scaling any loop body that already contains a scaled wait. | Current hook source and live switch trace E-023 | High |
| F-022 | Player-position visual domain | The player-position counter at `+0x1D54` is initialized from `Scheduler+0xC4`, decremented at `0x0064F0C6`, converted to an asset frame at `0x0064EF43`, and also used as a normalized ratio at `0x0064F75B` and `0x0064FD2E`. The current QPC 60-Hz decrement gate preserves approximate wall duration but leaves the state and ratios changing only 60 times per second. A smooth conversion requires a scaled native counter, a matching scaled denominator, and authored mapping only at the asset-frame sink. | Live IDA trace E-024 | High |
| F-023 | Stage timing split | Stage transform/color timelines in `0x00644000` consume absolute milliseconds and should continue at native cadence. Only the clip-mask lookup derives an index from `current_ms / Tune+0x18`; mapping that final index back to authored 60 Hz at `0x00644054` is the correct narrow conversion. Gating the stage renderer would destroy the intended high-FPS smoothness. | Live IDA traces E-020 and E-022 | High |
| F-024 | Other target-domain constants | The three consumers of `0x006F4604` convert native palette/lane counters to and from milliseconds; the palette cap and normalizer are already scaled consistently. The sole `0x006FC280` consumer converts scaled native counters to seconds. These constants should remain `1000/target_fps` and `1/target_fps`; reverting them globally would break the target-rate simulation paths rather than fix authored assets. | Live IDA constant-xref trace E-025 | High |
| F-025 | Stage-BGM preload gate | `CStageBgmTask+0x2C` counts from 0 to state label `0x12`, after which the same field is a state-machine selector. Gating only its pre-`0x12` increment at `0x0061001A` preserves the original preload delay without throttling the complete BGM task or altering later state labels. | Live IDA trace E-025 and current hook source | High |
| F-026 | Consumer audit completeness | All 26 live xrefs that consume `Tune+0x18` in gameplay visual/render code are classified: 12 are authored lifetime/index sinks that need a local 60-Hz conversion, while 14 are target-domain calculations or already have a final authored mapping. No `Tune+0x18` visual consumer remains unclassified in the current IDB. | Machine comparison of E-022 against the proposed site matrix, 2026-07-19 | High |
| F-027 | start2 login screen ownership | `start2_xfl` contains the mandatory card-use decision state under root `imc_card_01`. The matching binary state family is `SELECT_NOCARD*`, with `jf_decision_card`, `jf_card_yes`, `jf_card_no`, and `jf_card_01_timeout`. | E-029 | High |
| F-028 | Superseded callback identity | `0x005A5E80` owns a different CStartTask timer/input state. It is not the mandatory login card-use page: a full session traversed that page while both probes at this callback recorded zero calls. | E-029 | High |
| F-029 | Correct login trace anchor | Static initializer `sub_69D700` binds the `SELECT_NOCARD_INIT` descriptor to `sub_5A4540`. Timer/input/2D tracing for the broken login page must begin there and follow the `SELECT_NOCARD*` descriptor sequence. | E-029 | High |
| F-030 | Shared IFBL runtime cadence | Sustained sampled elapsed-time callbacks (`select-music`, `difficulty`, and `game-over`) run near 240 calls per second at a validated 240 FPS cap and accumulate approximately one second of delta per real second. There is no global IFBL callback-service slowdown. | E-029 | High |
| F-031 | Shared MovieClip owner | Automatic MovieClip progression is performed by the shared `Anim::DrawTraverse` visitor: mode 1 calls the `+0x134` forward wrapper, which reaches the `+0x150` one-frame primitive at `0x004DF940`. The five vtable occurrences of that primitive belong to one MovieClip inheritance/adaptor chain, so the current callee hook covers every ordinary MovieClip instance rather than a single scene. | E-027 | High |
| F-032 | Shared elapsed-time callback set | The 2D/sequence subsystem has 21 unique IFBL opcode-1 callbacks that consume the global elapsed delta, including card countdown/input, song-selection easing, fades, and other menu/transition countdowns. Several callbacks combine elapsed-time progress with input or frame-counted repeat in the same invocation. | E-027 | High |
| F-033 | 2D patch safety boundary | A global delta change, opcode-1 throttle, or complete sequence-task gate would affect the whole callback set, while a MovieClip-only gate cannot correct their native seconds timers. Runtime cadence must be measured at the shared service boundary before changing timing behavior. | E-027 | High |
| F-034 | Login descriptor opcode | The mandatory `SELECT_NOCARD*` login sequence uses IFBL opcode `0x27` conditional callbacks, not opcode 1. The shared opcode-1 diagnostic could not observe this page by construction. | E-030 | High |
| F-035 | Login input callback | `SELECT_NOCARD_INIT` binds `sub_5A4540`, which dispatches decision/navigation actions 14, 10, and 11 according to the current IFBL frame index. It does not own the visible countdown. | E-030 | High |
| F-036 | Login countdown clock | `sub_5A4B60` initializes the login/read-card timer to 30.9 seconds. `sub_5A3AC0` services it by subtracting the current global elapsed delta through `sub_5A37D0`/`sub_5A35E0` and reports expiration through `sub_5A3530`. This is an absolute-seconds timer, not an integer-frame wait. | E-030 | High |
| F-037 | Login trace boundary | Opcode `0x27` invokes its callback directly and conditionally resolves a label; `CStartTask::Update` calls the sequence state machine on each task invocation. The unresolved clock boundary is the scheduler cadence reaching `CStartTask::Update`. | E-030 | High |
| F-038 | CStartTask registration | `sub_60D040` constructs `CStartTask` and registers it in the shared task list with IDs 500/500. The task system's update subscriber is registered at priority 10. | E-031 | High |
| F-039 | CStartTask update dispatch | The update task traversal at `0x0045C250` walks active tasks and, when the node `+0x18` suspension/delay field is zero, dispatches virtual slot `+0x0C`. That slot is `sub_5A40A0` for CStartTask and reaches the active IFBL state machine. | E-031 | High |
| F-040 | Scheduler hypothesis retired | No CStartTask-specific authored-60-Hz gate exists in the scheduler path. The reported login slowdown cannot be assigned to a 60-Hz CStartTask scheduler without contrary evidence from another boundary. | E-031 | High |
| F-041 | SELECT_NOCARD polling loop | The active login loop runs the absolute timer callback and three input checks, executes an opcode-`0x11` integer wait with value 1 at descriptor `0x0081CBE8`, then branches back to `SELECT_NOCARD`. | E-032 | High |
| F-042 | Login root cause | The loader's global IFBL wait hook at `0x006309D4` scales that cooperative wait from 1 to 4 at 240 FPS. Consequently the loop subtracts one 1/240-second delta and samples one-update input edges only once per four updates, producing the observed 4x timer slowdown and dropped-feeling input. | E-032 | High |
| F-043 | IFBL wait domain split | Opcode-`0x11` values are not uniformly authored visual durations. At least the `SELECT_NOCARD` value-1 wait is a cooperative polling yield and must remain one native update. | E-032 | High |
| F-044 | Complete opcode-0x11 inventory | The executable contains 34 statically initialized opcode-`0x11` waits: 22 value-1 waits, 10 value-0 waits, and 2 value-15 waits. No other executable initializer stores this opcode into an IFBL descriptor. | E-033 | High |
| F-045 | Poll-yield family | All 22 value-1 descriptors are immediately followed by a branch back to their polling label. They cover the card read/select/reissue/takeover/use-card/no-card flows plus credit, bonus, and start-game polling. All must remain one native update. | E-033 | High |
| F-046 | Authored IFBL waits | The only positive opcode-`0x11` waits greater than 1 are two value-15 card-name yes/no visual pauses. These retain target-rate scaling to preserve their original 250 ms duration. | E-033 | High |
| F-047 | Proven IFBL production rule | The shared wait hook must preserve values 0 and 1 and scale only values greater than 1. This matches every static descriptor in the current binary. | E-033 | High |
| F-048 | Production correction | `ScaleIfblIntegerWait` now preserves 0/1 and scales only values greater than 1 at the shared interpreter store. The two obsolete diagnostic hooks and their runtime state/logging were removed. | E-034 | High |
| F-049 | Build/deployment | The x86 production DLL and focused timing test built successfully; the test exited zero. DLL SHA256 `3EA2BF5238E1F9795EC99B91AA8EF1531D80740C0DB7ADD61B574CC11BB9628E` is deployed to `H:\gc\iDmacDrv32.dll`. | E-034 | High |
| F-050 | Runtime status | Static root cause and production implementation are complete; 240 FPS operator acceptance of the login page and related polling families remains pending. | E-034 | High |
| F-051 | Runtime acceptance | The operator tested the deployed DLL at 240 FPS, reported that the current behavior seems fine, and requested commit and merge. This accepts the deployed correction without implying that a quantitative four-rate matrix was captured. | E-035 | High |
